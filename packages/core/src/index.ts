/**
 * @sovereignclaw/core - agent runtime, inference adapter, tool runtime.
 *
 * Step 1.1 placeholder. Real exports land in Step 1.3 per the Phase 1 plan:
 *   - Agent class
 *   - sealed0GInference adapter (Router-based, x_0g_trace.tee_verified pinned)
 *   - Tool interface and runtime
 *   - Lifecycle hooks
 *   - Event emitter
 */
export const VERSION = '0.0.0';
